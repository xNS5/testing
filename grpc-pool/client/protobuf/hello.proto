syntax = "proto3";

package hello;

option go_package = ".";

service Hello {
  rpc Hello(Request) returns (Response) {}
}

message Request {
  optional int32 timeout = 1;
  optional string msg = 2;
}

message Response {
    string res = 1;
}